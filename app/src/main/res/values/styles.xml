<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BtnNext">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/common_tv_white</item>
        <item name="android:background">@drawable/btn_next_blue</item>
        <item name="android:layout_marginLeft">@dimen/common_margin_left</item>
        <item name="android:layout_marginRight">@dimen/common_margin_right</item>
        <item name="android:textSize">18sp</item>
        <item name="android:padding">10dp</item>
    </style>

    <!-- 自定义邀请码界面菜单dialog -->
    <style name="invitation_dialog" parent="android:Theme.Dialog">
        <!-- 设置dialog的背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否显示title -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 也是个drawable，定义contentoverlay的背景 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 背景是否模糊显示 -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowFrame">@null</item> <!-- Dialog的windowFrame框为无 -->
    </style>



</resources>
